# PDFClarity GUI 开发 —— M0 具体实现方案

| 项目 | 内容 |
| ---- | ---- |
| 文档状态 | 实施方案（待评审后开工） |
| 版本 | v1.0 |
| 更新日期 | 2026-08-18 |
| 对应里程碑 | 图形界面思路 v2.2 中的 **M0** |
| 前置文档 | `__文档__/图形界面思路.md`、`__文档__/CPU 并发加速方案.md` |

---

## 0. M0 目标与验收标准

### 0.1 目标

M0 完成「后端地基」，不涉及任何前端界面：

1. **S1**：把 `run.py` 重构为可导入的 `core/` 模块，CLI 行为零回归；
2. **S2**：`process_one` 事件回调化（`on_stage` / `on_progress` / `on_done`），CLI 模式传空回调、行为不变；
3. **S3**：后端全局状态字典 + 锁，任务在后台线程运行；
4. **S7**：启动自检（引擎存在性、GPU 枚举、nvidia-smi）；
5. FastAPI 骨架 + `POST /api/jobs` 跑通全流水线 + 最小 `GET /api/state`（供验证进度）。

### 0.2 验收标准（M0 完成的判定）

| # | 验收项 | 判定方法 |
| ---- | ---- | ---- |
| A1 | CLI 零回归 | `python run.py <小PDF> 2 2160` 与重构前的输出、成品行为一致（见 §6） |
| A2 | 服务可启动 | `python app.py` 后 `GET /api/state` 返回 JSON，`engine_ok` 与 `gpus` 正确 |
| A3 | API 跑通全流水线 | `POST /api/jobs` 提交小 PDF，轮询 `/api/state` 观察到 `phase` 依次为 extract → upscale → rebuild → done，且 `output_path` 指向存在的成品 |
| A4 | 防重入 | 任务运行中再次 `POST /api/jobs` 返回 409 |
| A5 | 引擎缺失保护 | 临时改名 `bin/realesrgan/` 后启动，`engine_ok=false` 且 `POST /api/jobs` 返回 400 |

### 0.3 明确不在 M0 范围（后续里程碑）

- `GET /api/logs` 轮询接口、前端轮询器 → **M1**；
- `POST /api/jobs/{id}/cancel`、单实例锁、崩溃残留清理 → **M4**；
- 缩略图接口、前端、pywebview、打包 → **M2/M3/M5**。

---

## 1. 现状代码盘点（改动基础）

### 1.1 `run.py` 符号 → 去向对照表

| 现状位置 | 符号 | 去向 |
| ---- | ---- | ---- |
| run.py L39-46 | `HERE` / `ENGINE_EXE` / `ENGINE_MAC` / `MODEL_DIR` / `EXTRACT` / `REBUILD` / `WORK_ROOT` / `OUT_ROOT` | `core/paths.py` |
| run.py L48-56 | `RESET` / `BOLD` / `DIM` / `RED` / `GREEN` / `YELLOW` / `CYAN` / `IS_TTY` | `core/statusbar.py` |
| run.py L59-65 | `enable_ansi()` | `core/statusbar.py` |
| run.py L68-72 | `env_bool()` | **留在** `run.py`（仅 CLI 用） |
| run.py L75-80 | `find_engine()` | `core/gpu.py` |
| run.py L83 | `GPU_RE` | `core/gpu.py` |
| run.py L86-112 | `probe_gpus()` | `core/gpu.py` |
| run.py L115-128 | `pick_gpu()` | `core/gpu.py` |
| run.py L131-134 | `count_png()` | `core/progress.py` |
| run.py L137-148 | `count_tmp_jpg()` | `core/progress.py` |
| run.py L151-166 | `pdf_page_count()` | `core/progress.py` |
| run.py L170-272 | `SysMonitor` | `core/monitor.py` |
| run.py L276-364 | `StatusBar` | `core/statusbar.py` |
| run.py L367-377 | `run_with_progress()` | `core/pipeline.py`（**签名扩展，见 §3.6**） |
| run.py L380-400 | `upscale()` | `core/pipeline.py`（同上） |
| run.py L403-467 | `process_one()` | `core/pipeline.py`（**新增回调，见 §3.6**） |
| run.py L470-598 | `main()` | **留在** `run.py` |
| run.py L601-602 | `if __name__ == "__main__"` | **留在** `run.py` |

### 1.2 保持不动的部分

- `scripts/extract_pages.py`、`scripts/rebuild_pdf.py`：**一行不改**（仍以子进程方式调用）；
- `bin/realesrgan/`、`work/`、`output/` 结构不变；
- 流水线三段命令的参数（zoom 2.0、--quality 90、--ref-pdf 等）不变。

### 1.3 关键注意点（重构的坑）

1. **`HERE` 语义变化**：旧 `run.py` 中 `HERE = dirname(abspath(__file__))` 是**项目根目录**；移到 `core/paths.py` 后 `__file__` 指向 `core/`，必须写成 `dirname(dirname(abspath(__file__)))`，否则所有路径错位；
2. **`IS_TTY` 的求值时机**：原代码在 `import run.py` 时对 `sys.stdout.isatty()` 求值；移入 `core/statusbar.py` 后仍在 `import` 时求值，时序不变，行为一致；
3. **禁止循环导入**：`core/pipeline.py` 依赖 `core/paths.py / gpu.py / progress.py / monitor.py / statusbar.py`，方向单一，无环；
4. **`process_one` 保持原签名风格**（全部参数保持位置/关键字兼容），只**追加**可选回调参数，避免破坏 `run.py` 现有调用。

---

## 2. 目标目录结构

```
PDFClarity/
├── app.py                  # [新增] M0 冒烟入口:启动 uvicorn(无 pywebview)
├── run.py                  # [修改] 薄封装,保留全部 CLI 行为
├── core/                   # [新增] 抽取的核心逻辑(CLI 与 GUI 共用)
│   ├── __init__.py         # 统一 re-export
│   ├── paths.py            # 路径常量
│   ├── gpu.py              # find_engine / probe_gpus / pick_gpu / GPU_RE
│   ├── progress.py         # count_png / count_tmp_jpg / pdf_page_count
│   ├── monitor.py          # SysMonitor
│   ├── statusbar.py        # 配色 / IS_TTY / enable_ansi / StatusBar
│   └── pipeline.py         # run_with_progress / upscale / process_one(回调版)
├── server/                 # [新增] FastAPI 后端
│   ├── __init__.py         # 空文件,标记为包
│   ├── jobs.py             # 全局状态字典 + 锁 + 任务线程 + 自检
│   └── main.py             # FastAPI 应用 / POST /api/jobs / GET /api/state
├── scripts/                # [不变]
├── bin/realesrgan/         # [不变]
├── work/  output/          # [不变,运行时生成]
└── environment.yml         # [修改] 追加 fastapi / uvicorn
```

---

## 3. 具体代码改动

### 3.1 新增 `core/paths.py`

```python
"""路径常量。所有脚本/服务均从本模块导入,避免硬编码。

注意:本文件位于 core/ 子目录,__file__ 指向 core/,项目根需再向上取一层。
"""
import os

HERE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))  # 项目根目录
ENGINE_EXE = os.path.join(HERE, "bin", "realesrgan", "realesrgan-ncnn-vulkan.exe")
ENGINE_MAC = os.path.join(HERE, "bin", "realesrgan", "realesrgan-ncnn-vulkan")
MODEL_DIR = os.path.join(HERE, "bin", "realesrgan", "models")
EXTRACT = os.path.join(HERE, "scripts", "extract_pages.py")
REBUILD = os.path.join(HERE, "scripts", "rebuild_pdf.py")
WORK_ROOT = os.path.join(HERE, "work")
OUT_ROOT = os.path.join(HERE, "output")
```

> 内容与原 run.py L39-46 一一对应，仅 `HERE` 多取一层（见 §1.3-1）。

### 3.2 新增 `core/gpu.py`

```python
"""GPU 探测与选择。"""
import os
import re
import subprocess

from core.paths import HERE, ENGINE_EXE, ENGINE_MAC, MODEL_DIR

GPU_RE = re.compile(r"^\s*\[\s*(\d+)\s+(.*?)\s+\]\s+queue", re.MULTILINE)


def find_engine():
    if os.path.isfile(ENGINE_EXE):
        return ENGINE_EXE
    if os.path.isfile(ENGINE_MAC):
        return ENGINE_MAC
    return None


def probe_gpus(engine):
    """运行一次引擎读取 Vulkan 设备列表(逻辑与旧 run.py L86-112 完全一致)。"""
    probe_in = os.path.join(HERE, "work", ".gpu_probe_src")
    probe_out = os.path.join(HERE, "work", ".gpu_probe_out")
    os.makedirs(probe_in, exist_ok=True)
    os.makedirs(probe_out, exist_ok=True)
    px = os.path.join(probe_in, "p.png")
    if not os.path.isfile(px):
        try:
            from PIL import Image
            Image.new("RGB", (4, 4), (128, 128, 128)).save(px)
        except Exception:
            return None
    cmd = [engine, "-i", probe_in, "-o", probe_out, "-n", "realesrgan-x4plus",
           "-s", "4", "-m", MODEL_DIR, "-g", "0", "-f", "png"]
    try:
        r = subprocess.run(cmd, stdout=subprocess.PIPE,
                           stderr=subprocess.STDOUT, timeout=120)
        text = r.stdout.decode("utf-8", errors="replace")
    except Exception:
        return None
    return [(int(m.group(1)), m.group(2).strip()) for m in GPU_RE.finditer(text)]


def pick_gpu(gpus):
    """优先独显(NVIDIA > 独立 A 卡 > 核显)。"""

    def score(name):
        n = name.lower()
        if "nvidia" in n or "geforce" in n or "rtx" in n or "gtx" in n:
            return 4
        if "radeon" in n and "graphics" not in n:
            return 3
        if "graphics" not in n and "uhd" not in n and "iris" not in n:
            return 2
        return 1

    return max(gpus, key=lambda g: (score(g[1]), -g[0]))[0]
```

> `find_engine`、`GPU_RE`、`probe_gpus`、`pick_gpu` 逻辑逐字保留，仅 import 改为来自 `core.paths`。

### 3.3 新增 `core/progress.py`

```python
"""进度计数与页数探测(全部为纯函数,无状态)。"""
import os


def count_png(d):
    if not os.path.isdir(d):
        return 0
    return len([f for f in os.listdir(d) if f.lower().endswith(".png")])


def count_tmp_jpg(out_root):
    """rebuild_pdf.py 在 output/_jpg_tmp_<pid>/ 生成临时 jpg,统计其数量即进度。"""
    n = 0
    try:
        for d in os.listdir(out_root):
            if d.startswith("_jpg_tmp_"):
                p = os.path.join(out_root, d)
                if os.path.isdir(p):
                    n += len([f for f in os.listdir(p) if f.lower().endswith(".jpg")])
    except OSError:
        pass
    return n


def pdf_page_count(pdf):
    """读取 PDF 页数;失败返回 None(进度百分比退化为页码)。"""
    try:
        import pymupdf as fitz
    except ImportError:
        try:
            import fitz
        except ImportError:
            return None
    try:
        d = fitz.open(pdf)
        n = d.page_count
        d.close()
        return n
    except Exception:
        return None
```

> 与原 run.py L131-166 逐字一致。

### 3.4 新增 `core/monitor.py`

```python
"""实时系统监控(GPU / CPU / 内存)。内容与原 run.py SysMonitor 一致。"""
import shutil
import subprocess
import threading


class SysMonitor:
    """后台线程采样系统指标。

    - GPU: 常驻 nvidia-smi 子进程,按行解析,每秒得到每块 NVIDIA 卡的指标;
    - CPU/内存: psutil 采样线程(每 1s);
    - 任一来源不可用时对应字段为 None,调用方显示 n/a。
    """

    def __init__(self):
        self._lock = threading.Lock()
        self._gpu = {}                       # idx -> dict(util, mem_used, mem_total, temp, power)
        self._cpu = None
        self._ram = None
        self._peaks = {"gpu": 0.0, "cpu": 0.0}
        self._stop = threading.Event()
        self._proc = None

        self._psutil = None
        try:
            import psutil as _p
            self._psutil = _p
        except Exception:
            pass

        nvsmi = shutil.which("nvidia-smi")
        if nvsmi:
            cmd = [nvsmi, "--query-gpu=index,utilization.gpu,memory.used,memory.total,"
                          "temperature.gpu,power.draw",
                   "--format=csv,noheader,nounits", "-l", "1"]
            try:
                self._proc = subprocess.Popen(cmd, stdout=subprocess.PIPE,
                                              stderr=subprocess.DEVNULL,
                                              text=True, encoding="utf-8",
                                              errors="replace", bufsize=1)
                threading.Thread(target=self._gpu_reader, daemon=True).start()
            except Exception:
                self._proc = None

        if self._psutil is not None:
            self._psutil.cpu_percent(None)   # 预热
            threading.Thread(target=self._sys_reader, daemon=True).start()

    @staticmethod
    def _f(s, default):
        try:
            return float(s)
        except (TypeError, ValueError):
            return default

    def _gpu_reader(self):
        while not self._stop.is_set():
            line = self._proc.stdout.readline()
            if not line:
                break
            parts = [p.strip() for p in line.split(",")]
            if len(parts) < 6:
                continue
            try:
                idx = int(parts[0])
            except ValueError:
                continue
            entry = {
                "util": self._f(parts[1], 0.0),
                "mem_used": self._f(parts[2], 0.0),
                "mem_total": self._f(parts[3], 0.0),
                "temp": self._f(parts[4], None),
                "power": self._f(parts[5], None),
            }
            with self._lock:
                self._gpu[idx] = entry
                self._peaks["gpu"] = max(self._peaks["gpu"], entry["util"])

    def _sys_reader(self):
        p = self._psutil
        while not self._stop.is_set():
            if self._stop.wait(1.0):
                break
            try:
                cpu = p.cpu_percent(None)
                ram = p.virtual_memory().percent
            except Exception:
                continue
            with self._lock:
                self._cpu, self._ram = cpu, ram
                self._peaks["cpu"] = max(self._peaks["cpu"], cpu)

    def sample(self):
        with self._lock:
            return {"gpu": dict(self._gpu), "cpu": self._cpu, "ram": self._ram,
                    "peaks": dict(self._peaks)}

    def reset_peaks(self):
        with self._lock:
            self._peaks = {"gpu": 0.0, "cpu": 0.0}

    def close(self):
        self._stop.set()
        if self._proc is not None:
            try:
                self._proc.terminate()
            except Exception:
                pass
```

> 与原 run.py L170-272 逐字一致。

### 3.5 新增 `core/statusbar.py`

```python
"""终端配色 / TTY 判断 / 单行状态栏。内容与原 run.py 对应部分一致。"""
import os
import sys
import time

# ---- 终端配色(Windows 10+ conhost / Windows Terminal 支持 VT 转义) ----
RESET = "\033[0m"
BOLD = "\033[1m"
DIM = "\033[2m"
RED = "\033[31m"
GREEN = "\033[32m"
YELLOW = "\033[33m"
CYAN = "\033[36m"
IS_TTY = sys.stdout.isatty()


def enable_ansi():
    """激活 Windows 控制台的 VT 转义(无输出副作用),非 Windows 直接跳过。"""
    if os.name == "nt" and IS_TTY:
        try:
            os.system("")
        except Exception:
            pass


class StatusBar:
    """阶段进度 + 系统指标 的单行状态栏。逻辑与原 run.py L276-364 一致。"""

    def __init__(self, refresh=0.4):
        self.refresh = refresh
        self._last = 0.0
        self._last_pct = -1

    @staticmethod
    def _bar(cur, total, width=24):
        frac = cur / total if total else 0
        filled = int(round(frac * width))
        return "[" + "█" * filled + "░" * (width - filled) + "]"

    @staticmethod
    def _num(s):
        try:
            return float(s)
        except (TypeError, ValueError):
            return None

    @staticmethod
    def _fmt_time(sec):
        sec = int(max(0, sec))
        return f"{sec // 60:02d}:{sec % 60:02d}"

    def _fmt_gpu(self, stats):
        gpu = stats.get("gpu") or {}
        if not gpu:
            return f"{DIM}GPU n/a{RESET}"
        segs = []
        multi = len(gpu) > 1
        for idx in sorted(gpu):
            e = gpu[idx]
            u = e.get("util") or 0.0
            col = GREEN if u >= 60 else (YELLOW if u >= 30 else DIM)
            label = f"GPU{idx}" if multi else "GPU"
            s = f"{col}{label} {u:3.0f}%{RESET}"
            if e.get("mem_total"):
                s += f" {e['mem_used'] / 1024:.1f}/{e['mem_total'] / 1024:.0f}G"
            temp, power = self._num(e.get("temp")), self._num(e.get("power"))
            if temp is not None:
                s += f" {temp:.0f}°C"
            if power is not None:
                s += f" {power:.0f}W"
            segs.append(s)
        return " · ".join(segs)

    def _fmt_sys(self, stats):
        cpu, ram = stats.get("cpu"), stats.get("ram")
        if cpu is None:
            cpu_s = f"{DIM}CPU n/a{RESET}"
        else:
            col = GREEN if cpu >= 60 else (YELLOW if cpu >= 30 else DIM)
            cpu_s = f"CPU {col}{cpu:3.0f}%{RESET}"
        ram_s = f"{ram:3.0f}%" if ram is not None else "n/a"
        return f"{cpu_s} · RAM {ram_s}"

    def render(self, stage, cur, total, t0, stats):
        now = time.time()
        pct = int(cur * 100 / total) if total else 0

        if not IS_TTY:                          # 重定向输出:稀疏纯文本行
            if (total is not None and cur >= total) or pct >= self._last_pct + 5:
                self._last_pct = pct
                print(f"    {stage}  {cur}/{total or '?'} ({pct}%)")
            return

        if now - self._last < self.refresh and (total is None or cur < total):
            return
        self._last = now

        line = (f"  {BOLD}{CYAN}{stage}{RESET}  {self._bar(cur, total)} "
                f"{cur}/{total or '?'} ({pct:3d}%)  "
                f"{self._fmt_gpu(stats)}  {self._fmt_sys(stats)}  "
                f"{DIM}{self._fmt_time(now - t0)}{RESET}")
        sys.stdout.write("\r" + line + "\033[K")
        sys.stdout.flush()

    def newline(self):
        if IS_TTY:
            sys.stdout.write("\r\033[K\n")
        else:
            self._last_pct = -1
        sys.stdout.flush()
```

### 3.6 新增 `core/pipeline.py`（核心：S2 事件回调化）

```python
"""流水线调度:run_with_progress / upscale / process_one。

相对旧 run.py 的改动:
- 三个函数均新增可选回调参数,CLI 传 None 时行为与旧版完全一致;
- run_with_progress 新增 phase(规范阶段键)参数,用于回调携带稳定标识;
- process_one 新增 on_stage / on_progress / on_done 三个回调。
"""
import os
import shlex
import subprocess
import sys
import time

from core.paths import WORK_ROOT, OUT_ROOT, MODEL_DIR, EXTRACT, REBUILD
from core.gpu import find_engine
from core.progress import count_png, count_tmp_jpg, pdf_page_count


def run_with_progress(cmd, log_path, count_fn, total, stage, monitor=None,
                      bar=None, phase=None, on_progress=None):
    """后台跑子进程(输出进日志),按 count_fn 轮询进度。

    - bar 为 None 时跳过终端状态栏渲染(GUI 模式);
    - on_progress(phase, cur, total) 供 GUI 写全局状态字典。
    """
    with open(log_path, "w", encoding="utf-8", errors="replace") as log:
        proc = subprocess.Popen(cmd, stdout=log, stderr=subprocess.STDOUT)
    t0 = time.time()
    while proc.poll() is None:
        cur = count_fn()
        if bar is not None and monitor is not None:
            bar.render(stage, cur, total, t0, monitor.sample())
        if on_progress is not None:
            on_progress(phase or stage, cur, total)
        time.sleep(0.4)
    cur = count_fn()
    if bar is not None and monitor is not None:
        bar.render(stage, cur, total, t0, monitor.sample())
        bar.newline()
    if on_progress is not None:
        on_progress(phase or stage, cur, total)
    return proc.returncode


def upscale(src, up, log_path, model, scale, gpu_ids, extra_args, monitor=None,
            bar=None, phase="upscale", on_progress=None):
    cmd = [find_engine(), "-i", src, "-o", up, "-n", model,
           "-s", str(scale), "-m", MODEL_DIR, "-g", gpu_ids, "-f", "png"]
    if extra_args:
        cmd += shlex.split(extra_args)
    print("    " + " ".join(cmd))
    total = count_png(src)
    rc = run_with_progress(cmd, log_path, lambda: count_png(up), total,
                           f"超分 x{scale}", monitor, bar, phase, on_progress)
    if rc != 0:
        # 某些环境(杀软/驱动/沙箱)可能在页面全部写完后仍让进程非零退出;
        # 只要页面齐全就按成功处理,否则才是真正的失败。
        if count_png(up) == total and total > 0:
            print("    !! WARN: 超分进程退出码非 0,但全部页面已生成,继续")
        else:
            return rc
    # page-count consistency check: 防止缺页成品
    up_count = count_png(up)
    if up_count != total:
        print(f"    !! WARN: 超分输出 {up_count} 页 != 源 {total} 页,成品可能缺页")
    return 0


def process_one(pdf, idx=None, total=None, scale=4, target_w=4320,
                model="realesrgan-x4plus", gpu_ids="0", extra_args="",
                skip_existing=True, extract_workers=1, monitor=None, bar=None,
                on_stage=None, on_progress=None, on_done=None):
    """处理单个 PDF,返回 bool。

    回调约定(均为可选,None 时不影响任何原有打印/行为):
    - on_stage(phase, label) : 阶段切换,phase ∈ {extract, upscale, rebuild}
    - on_progress(phase, cur, total) : 每次轮询(约 0.4s)
    - on_done(ok, out_path, elapsed, per_page) : 任务结束(含 SKIP 分支)
    """
    tag = f"[{idx}/{total}] " if idx else ""
    base = os.path.splitext(os.path.basename(pdf))[0]
    work = os.path.join(WORK_ROOT, base)
    src = os.path.join(work, "src")
    up = os.path.join(work, "up")
    out = os.path.join(OUT_ROOT, base + "_clear.pdf")

    if skip_existing and os.path.isfile(out):
        print(f"==> {tag}SKIP (output exists): {out}")
        if on_done is not None:
            on_done(True, out, 0.0, "skip")
        return True

    print("=" * 72)
    print(f"==> {tag}Processing: {pdf}", flush=True)
    os.makedirs(src, exist_ok=True)
    os.makedirs(up, exist_ok=True)
    os.makedirs(OUT_ROOT, exist_ok=True)

    # 清理上次运行可能残留的中间页(中断/重跑时旧页会混进成品)
    for d in (src, up):
        for f in os.listdir(d):
            if f.lower().endswith((".png", ".jpg", ".jpeg")):
                try:
                    os.remove(os.path.join(d, f))
                except OSError:
                    pass

    if monitor is not None:
        monitor.reset_peaks()
    t_start = time.time()
    pages = pdf_page_count(pdf)                # 可能为 None

    def stage(phase, label):
        if on_stage is not None:
            on_stage(phase, label)

    stage("extract", "提取")
    print(f"  [1/3] 提取页面 (zoom 2.0, {extract_workers} 进程)", flush=True)
    rc = run_with_progress([sys.executable, EXTRACT, pdf, src, "--zoom", "2.0",
                            "--workers", str(extract_workers)],
                           os.path.join(work, "extract.log"),
                           lambda: count_png(src), pages, "提取", monitor, bar,
                           "extract", on_progress)
    if rc != 0:
        print(f"  !! 提取失败 (exit {rc}, 见 {os.path.join(work, 'extract.log')})")
        if on_done is not None:
            on_done(False, None, time.time() - t_start, None)
        return False

    stage("upscale", f"超分 x{scale}")
    print(f"  [2/3] 超分 x{scale} ({model}, GPU: {gpu_ids})", flush=True)
    rc = upscale(src, up, os.path.join(work, "upscale.log"), model, scale,
                 gpu_ids, extra_args, monitor, bar, "upscale", on_progress)
    if rc != 0:
        print(f"  !! 超分失败 (exit {rc}, 见 {os.path.join(work, 'upscale.log')})")
        if on_done is not None:
            on_done(False, None, time.time() - t_start, None)
        return False

    stage("rebuild", "重建")
    print(f"  [3/3] 重建 PDF (目标宽度 {target_w}px)", flush=True)
    rc = run_with_progress([sys.executable, REBUILD, up, out, "--ref-pdf", pdf,
                            "--target-width", str(target_w), "--quality", "90"],
                           os.path.join(work, "rebuild.log"),
                           lambda: count_tmp_jpg(OUT_ROOT), pages, "重建", monitor,
                           bar, "rebuild", on_progress)
    if rc != 0:
        print(f"  !! 重建失败 (exit {rc}, 见 {os.path.join(work, 'rebuild.log')})")
        if on_done is not None:
            on_done(False, None, time.time() - t_start, None)
        return False

    dt = time.time() - t_start
    per_page = f"{dt / pages:.1f}s" if pages else "n/a"
    if monitor is not None:
        peaks = monitor.sample()["peaks"]
        print(f"  -> 完成: {out}")
        print(f"     用时 {dt:.1f}s | 每页 {per_page} | "
              f"峰值 GPU {peaks['gpu']:.0f}% / CPU {peaks['cpu']:.0f}%")
    else:
        print(f"  -> 完成: {out}")
        print(f"     用时 {dt:.1f}s | 每页 {per_page}")
    if on_done is not None:
        on_done(True, out, dt, per_page)
    return True
```

> **CLI 兼容性核对**：CLI 下 `bar`/`monitor` 均传入、三个回调均为 None，则 `bar.render` 每次调用、`print` 顺序、返回值路径与旧 `run.py` 完全一致；`process_one` 失败分支里多出的 `on_done` 调用为 None 短路，无副作用。

### 3.7 新增 `core/__init__.py`

```python
"""core 包:CLI 与 GUI 共用的核心逻辑。"""
from core.paths import (HERE, WORK_ROOT, OUT_ROOT, ENGINE_EXE, ENGINE_MAC,
                        MODEL_DIR, EXTRACT, REBUILD)
from core.gpu import find_engine, probe_gpus, pick_gpu
from core.progress import count_png, count_tmp_jpg, pdf_page_count
from core.monitor import SysMonitor
from core.statusbar import IS_TTY, StatusBar, enable_ansi
from core.pipeline import process_one, upscale, run_with_progress

__all__ = [
    "HERE", "WORK_ROOT", "OUT_ROOT", "ENGINE_EXE", "ENGINE_MAC",
    "MODEL_DIR", "EXTRACT", "REBUILD",
    "find_engine", "probe_gpus", "pick_gpu",
    "count_png", "count_tmp_jpg", "pdf_page_count",
    "SysMonitor", "IS_TTY", "StatusBar", "enable_ansi",
    "process_one", "upscale", "run_with_progress",
]
```

### 3.8 修改 `run.py`（薄封装）

删除 §1.1 对照表中所有「移入 core/」的代码，保留 CLI 编排逻辑，整体为：

```python
#!/usr/bin/env python3
"""PDFClarity — make low-resolution / blank-in-Skim scanned PDFs sharper.

CLI 入口。核心逻辑已抽入 core/ 模块,本文件只保留参数解析与批处理编排,
行为与旧版完全一致(含单行状态栏与退出码)。

Usage:
  python run.py <input.pdf|folder> [model-scale] [target-width]
"""

import argparse
import os
import shutil
import subprocess
import sys

from core import (find_engine, probe_gpus, pick_gpu, SysMonitor, StatusBar,
                  enable_ansi, process_one, OUT_ROOT)


def env_bool(name, default):
    v = os.environ.get(name)
    if v is None:
        return default
    return v.strip().lower() in ("1", "true", "yes", "on")


def main():
    ap = argparse.ArgumentParser(
        description=__doc__,
        formatter_class=argparse.RawDescriptionHelpFormatter)
    ap.add_argument("input", help="input PDF file, or folder of PDFs (batch)")
    ap.add_argument("scale", nargs="?", type=int, default=4,
                    help="upscale factor (2 or 4, default 4)")
    ap.add_argument("target_width", nargs="?", type=int, default=4320,
                    help="final page width in px; 0 = keep full upscaled size (default 4320)")
    args = ap.parse_args()

    model = os.environ.get("MODEL", "realesrgan-x4plus")
    recursive = env_bool("RECURSIVE", False)
    skip_existing = env_bool("SKIP_EXISTING", True)
    gpu_ids = os.environ.get("GPU_IDS")
    extra_args = os.environ.get("EXTRA_ARGS", "")
    try:
        extract_workers = int(os.environ.get(
            "EXTRACT_WORKERS", str(min(8, os.cpu_count() or 4))))
    except ValueError:
        extract_workers = 1
    extract_workers = max(1, extract_workers)

    # Windows console / child python: 统一 UTF-8,避免中文按 GBK 输出
    for stream in (sys.stdout, sys.stderr):
        try:
            stream.reconfigure(encoding="utf-8")
        except Exception:
            pass
    os.environ.setdefault("PYTHONIOENCODING", "utf-8")
    enable_ansi()

    engine = find_engine()
    if engine is None:
        print("!! 找不到超分引擎:")
        print("   - Windows: 下载官方 Windows 版并放入 bin/realesrgan/:")
        print("     https://github.com/xinntao/Real-ESRGAN/releases/download/v0.2.5.0/realesrgan-ncnn-vulkan-20220424-windows.zip")
        print("   - macOS: 确认 bin/realesrgan/realesrgan-ncnn-vulkan 存在且可执行 (chmod +x)")
        sys.exit(1)

    # GPU 自动选择:未显式设置 GPU_IDS 时,枚举 Vulkan 设备并优先选独显(NVIDIA)
    if gpu_ids is None:
        gpus = probe_gpus(engine)
        if gpus:
            picked = str(pick_gpu(gpus))
            names = ", ".join(f"[{i} {n}]" for i, n in gpus)
            print(f"==> 检测到 GPU: {names}")
            if picked != "0":
                print(f"==> 自动选择 GPU {picked} (默认 0 通常是集显;可用 GPU_IDS 覆盖)")
            gpu_ids = picked
        else:
            print("!! 未枚举到 Vulkan 设备(探测失败),回退 GPU 0;"
                  "若占用率上不去,请检查显卡驱动 / 用 GPU_IDS 指定")
            gpu_ids = "0"

    # GPU/驱动预检(警示级,不阻断;权威判断交给引擎自身报错)
    if shutil.which("nvidia-smi"):
        print("==> NVIDIA GPU:", flush=True)
        try:
            subprocess.run(["nvidia-smi", "-L"], check=True)
        except Exception:
            pass
    else:
        print("!! 未检测到 nvidia-smi(仅警示,不阻断;AMD/Intel 走 Vulkan 可忽略,macOS 可忽略)")

    monitor = SysMonitor()
    bar = StatusBar()
    try:
        if os.path.isfile(args.input):
            ok = process_one(args.input, scale=args.scale, target_w=args.target_width,
                             model=model, gpu_ids=gpu_ids, extra_args=extra_args,
                             skip_existing=skip_existing,
                             extract_workers=extract_workers,
                             monitor=monitor, bar=bar)
            print()
            print("All done. Output in:", OUT_ROOT)
            sys.exit(0 if ok else 1)

        if os.path.isdir(args.input):
            pdfs = []
            for root, dirs, files in os.walk(args.input):
                if not recursive and root != args.input:
                    dirs[:] = []          # top-level only unless RECURSIVE=1
                for f in files:
                    if f.lower().endswith(".pdf"):
                        pdfs.append(os.path.join(root, f))
            pdfs.sort()
            if not pdfs:
                print("No PDF files found in:", args.input)
                sys.exit(1)
            print(f"Found {len(pdfs)} PDF(s) in: {args.input}")
            if recursive:
                print("(recursive scan on)")

            ok = fail = 0
            failed = []
            for i, f in enumerate(pdfs, 1):
                try:
                    if process_one(f, idx=i, total=len(pdfs), scale=args.scale,
                                   target_w=args.target_width, model=model,
                                   gpu_ids=gpu_ids, extra_args=extra_args,
                                   skip_existing=skip_existing,
                                   extract_workers=extract_workers,
                                   monitor=monitor, bar=bar):
                        ok += 1
                    else:
                        fail += 1
                        failed.append(f)
                except KeyboardInterrupt:
                    raise
                except Exception as e:
                    fail += 1
                    failed.append(f)
                    print(f"  !! FAILED: {f} ({e})")

            print()
            print("=" * 72)
            print(f"Batch finished: {ok} succeeded, {fail} failed (of {len(pdfs)}).")
            if failed:
                print("Failed files:")
                for f in failed:
                    print("  -", f)
            print("Output in:", OUT_ROOT)
            sys.exit(0 if fail == 0 else 1)

        print("Input not found (not a file or folder):", args.input)
        sys.exit(1)
    finally:
        monitor.close()


if __name__ == "__main__":
    main()
```

> 改动的**唯一**差异：`import` 区换成 `from core import ...`；删除所有被搬走的定义。`main()` 主体（UTF-8 设置、GPU 自动选择、nvidia-smi 预检、单文件/批处理编排、退出码、finally 关闭 monitor）逐行保留。

### 3.9 新增 `server/__init__.py`

```python
# 空文件:标记 server 为 Python 包
```

### 3.10 新增 `server/jobs.py`（核心：S3 全局状态字典 + 任务线程）

```python
"""任务调度:全局状态字典 + 后台任务线程。

线程模型(设计文档 4.3 S3):
- 任务线程(daemon) 跑 process_one,通过回调写 STATE;
- 指标采样线程 每 1s 把 SysMonitor.sample() 快照写入 STATE;
- HTTP 处理器只读 snapshot()(深拷贝),写读均由 threading.Lock 保护。
"""
import copy
import shutil
import threading
import time
import uuid

from core import SysMonitor, find_engine, probe_gpus, pick_gpu, process_one

_lock = threading.Lock()
STATE = {
    "running": False,          # 是否有任务在运行
    "job_id": None,
    "pdf": None,
    "phase": "idle",           # idle | extract | upscale | rebuild | done | error
    "stage_label": "",         # 供界面显示的中文阶段名
    "page": 0,                 # 当前完成页数
    "total": None,             # 总页数(探测失败为 None)
    "percent": 0,
    "message": "",
    "output_path": None,
    "error": None,
    "started_at": None,
    # 系统指标快照(由 _metrics_loop 周期写入)
    "metrics": {"gpu": {}, "cpu": None, "ram": None,
                "peaks": {"gpu": 0.0, "cpu": 0.0}},
    # 自检结果(S7)
    "gpus": [],                # [(idx, name), ...]
    "gpu_ids": None,           # 自动选择结果;POST /api/jobs 未指定时使用
    "engine_ok": False,
    "nvidia_smi": False,
}

monitor = None                 # SysMonitor 单例,init() 中创建


def _update(**kw):
    with _lock:
        STATE.update(kw)


def snapshot():
    """返回 STATE 的深拷贝,供 HTTP 层返回。"""
    with _lock:
        return copy.deepcopy(STATE)


def init():
    """应用启动时调用一次:创建监控实例 + 自检 + 启动指标采样线程。"""
    global monitor
    monitor = SysMonitor()
    self_check()
    threading.Thread(target=_metrics_loop, daemon=True).start()


def self_check():
    """S7:引擎存在性 / GPU 枚举与自动选择 / nvidia-smi 可用性。"""
    engine = find_engine()
    ok = engine is not None
    gpus = probe_gpus(engine) if ok else []
    gpu_ids = str(pick_gpu(gpus)) if gpus else ("0" if ok else None)
    _update(engine_ok=ok, gpus=gpus, gpu_ids=gpu_ids,
            nvidia_smi=shutil.which("nvidia-smi") is not None)


def _metrics_loop():
    while True:
        time.sleep(1.0)
        if monitor is None:
            continue
        _update(metrics=monitor.sample())


def start_job(job_id, params):
    """启动任务线程。调用前需已通过 snapshot() 校验未在运行。"""
    with _lock:
        if STATE["running"]:
            raise RuntimeError("已有任务在运行")
    threading.Thread(target=_job_thread, args=(job_id, params), daemon=True).start()


def _job_thread(job_id, params):
    pdf = params["pdf"]

    def on_stage(phase, label):
        _update(phase=phase, stage_label=label,
                message=f"正在{label}", error=None)

    def on_progress(phase, cur, total):
        _update(phase=phase, page=cur, total=total,
                percent=int(cur * 100 / total) if total else 0)

    def on_done(ok, out_path, elapsed, per_page):
        _update(running=False,
                phase="done" if ok else "error",
                output_path=out_path,
                message="完成" if ok else "失败",
                error=None if ok else "阶段失败,详见 work/<书名>/*.log")

    try:
        gpu_ids = params.get("gpu_ids")
        if not gpu_ids:
            with _lock:
                gpu_ids = STATE["gpu_ids"] or "0"
        _update(running=True, job_id=job_id, pdf=pdf, phase="extract",
                stage_label="提取", page=0, total=None, percent=0,
                message="开始处理", error=None, output_path=None,
                started_at=time.time())
        process_one(
            pdf,
            scale=params.get("scale", 4),
            target_w=params.get("target_width", 4320),
            model=params.get("model", "realesrgan-x4plus"),
            gpu_ids=gpu_ids,
            extra_args=params.get("extra_args", ""),
            skip_existing=params.get("skip_existing", True),
            extract_workers=params.get("extract_workers", 1),
            monitor=monitor,
            bar=None,                          # GUI 无终端状态栏
            on_stage=on_stage,
            on_progress=on_progress,
            on_done=on_done,
        )
    except Exception as e:
        _update(running=False, phase="error",
                error=str(e), message=f"任务异常: {e}")
```

### 3.11 新增 `server/main.py`

```python
"""FastAPI 应用:FastAPI 骨架 + POST /api/jobs + 最小 GET /api/state。

M0 只交付可验证的最小接口;完整轮询接口(GET /api/logs)在 M1 实现。
"""
import os
import uuid

from fastapi import FastAPI, HTTPException
from pydantic import BaseModel, Field

from server import jobs

app = FastAPI(title="PDFClarity", version="0.1.0")


class JobRequest(BaseModel):
    pdf: str
    scale: int = Field(4, ge=2, le=4)          # 2 或 4
    target_width: int = Field(4320, ge=0)       # 0 = 保留完整尺寸
    model: str = "realesrgan-x4plus"
    gpu_ids: str | None = None                 # None = 用自检自动选择结果
    extra_args: str = ""
    extract_workers: int = Field(1, ge=1)
    skip_existing: bool = True


@app.on_event("startup")
def _startup():
    jobs.init()


@app.get("/api/state")
def get_state():
    """返回全局状态快照(任务进度 + 系统指标 + 自检结果)。"""
    return jobs.snapshot()


@app.post("/api/jobs")
def create_job(req: JobRequest):
    """创建超分任务,后台线程执行。成功返回 job_id,冲突返回 409。"""
    st = jobs.snapshot()
    if st["running"]:
        raise HTTPException(409, "已有任务在运行")
    if not st["engine_ok"]:
        raise HTTPException(400, "超分引擎缺失,无法开始(检查 bin/realesrgan/)")
    if not os.path.isfile(req.pdf) or not req.pdf.lower().endswith(".pdf"):
        raise HTTPException(400, "pdf 路径不存在或不是 PDF 文件")

    job_id = f"{int(time.time())}-{uuid.uuid4().hex[:8]}"
    try:
        jobs.start_job(job_id, req.model_dump())
    except RuntimeError as e:
        raise HTTPException(409, str(e))
    return {"job_id": job_id, "status": "started"}
```

### 3.12 新增 `app.py`（M0 冒烟入口）

```python
"""M0 冒烟入口:仅启动本地 HTTP 服务,供 curl / PowerShell 验证。

pywebview 桌面壳在 M2 里程碑接入;本文件后续会改为「启动 uvicorn + 打开窗口」。
"""
import uvicorn

if __name__ == "__main__":
    uvicorn.run("server.main:app", host="127.0.0.1", port=8000, log_level="info")
```

> 端口冲突处理（顺延）不在 M0 范围，固定 8000，冲突时手动换端口。

### 3.13 修改 `environment.yml`（依赖变更）

```yaml
name: pdfclarity
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - pymupdf
      - pillow
      - tqdm
      - psutil
      - fastapi
      - "uvicorn[standard]"
```

> 若环境为已有 conda env，可直接 `pip install fastapi "uvicorn[standard]"`。

---

## 4. 全局状态字典与线程模型

### 4.1 `STATE` 字段表（`server/jobs.py`）

| 字段 | 类型 | 含义 | 写入方 | 读取方 |
| ---- | ---- | ---- | ---- | ---- |
| `running` | bool | 是否有任务在运行 | 任务线程 | 校验/界面 |
| `job_id` | str | 当前任务 id | 任务线程 | 界面 |
| `pdf` | str | 输入 PDF 路径 | 任务线程 | 界面 |
| `phase` | str | `idle/extract/upscale/rebuild/done/error` | 任务线程 | 界面 |
| `stage_label` | str | 中文阶段名 | 回调 `on_stage` | 界面 |
| `page` / `total` / `percent` | int | 进度 | 回调 `on_progress` | 界面 |
| `message` / `error` | str | 状态文案 / 错误信息 | 任务线程 | 界面 |
| `output_path` | str | 成品路径(done 后) | 回调 `on_done` | 界面 |
| `started_at` | float | 开始时间戳 | 任务线程 | 界面(用时) |
| `metrics` | dict | GPU/CPU/内存快照 | 指标采样线程 | 界面 |
| `gpus` / `gpu_ids` | list/str | 自检结果 | `self_check()` | 参数区/界面 |
| `engine_ok` / `nvidia_smi` | bool | 自检结果 | `self_check()` | 开始按钮/界面 |

### 4.2 线程模型

```
uvicorn 主线程(asyncio)
 ├── GET /api/state  → jobs.snapshot()(加锁读,返回深拷贝)
 └── POST /api/jobs  → jobs.start_job() → 创建 daemon 任务线程(立即返回)

任务线程(每任务一个,daemon=True)
 └── process_one(monitor=共享, bar=None, 三个回调)
       ├── on_stage   → _update(phase, stage_label, …)
       ├── on_progress→ _update(page, total, percent, …)
       └── on_done    → _update(running=False, phase=done/error, …)

指标采样线程(常驻,daemon=True)
 └── 每 1s: _update(metrics=monitor.sample())
```

### 4.3 并发安全要点

1. 所有写操作经 `_update()`（持 `_lock`）；
2. 读一律走 `snapshot()`（持锁深拷贝），HTTP 层拿到的快照不会出现半写状态；
3. `SysMonitor` 内部自带锁，`sample()` 可被任务线程（`peaks`）与指标采样线程并发调用，安全；
4. 防重入双保险：`create_job` 先查 `snapshot()["running"]`（返回 409），`start_job` 内再持锁二次校验（竞态兜底）；
5. M0 任务线程为 daemon：进程退出时随主进程终止，无需等待；**取消逻辑（S4）在 M4 引入**，届时需改为可中断。

---

## 5. 实施顺序

| 步骤 | 内容 | 完成后验证 |
| ---- | ---- | ---- |
| 1 | 新建 `core/` 五个模块 + `__init__.py`（§3.1-3.7） | `python -c "from core import process_one; print(process_one)"` 无导入错误 |
| 2 | 重写 `run.py`（§3.8） | 见 §6 CLI 回归 |
| 3 | 新建 `server/`（§3.9-3.11） | `python -c "from server.main import app; print(app.title)"` |
| 4 | 新建 `app.py`（§3.12）、更新 `environment.yml`（§3.13）并安装依赖 | `python app.py` 启动无报错 |
| 5 | API 冒烟（§7） | A2~A5 全部通过 |

---

## 6. CLI 回归验证（验收 A1）

1. 准备一个小 PDF（≤3 页），如复用已有成品或自建 2 页样本；
2. 执行（重构前先记录一份输出/成品留底）：

```powershell
python run.py "d:\Code\PDFClarity_push\work\<小样本>.pdf" 2 2160
```

3. 逐项核对与旧版一致：
   - 状态栏格式（`提取  [█░░░…] cur/total (pct%)  GPU …  CPU …  用时`）；
   - 三段阶段标题 `[1/3]` / `[2/3]` / `[3/3]`；
   - 结尾 `All done. Output in:` 与 `output/` 下成品路径、页数、大小；
   - 退出码 0；
4. 再验证**批处理**分支（目录参数）与 `SKIP_EXISTING` 分支各一次；
5. 失败分支抽样：用一个损坏的 PDF，确认 `!! 提取失败` 与退出码 1 不变。

> 回归基准：本仓库 `work/27张宇基础30讲概率.pdf`（173 页）跑一次全流程，对比重构前后 `upscale.log` 里引擎命令与成品页数一致（时间长短不计入验收）。

---

## 7. API 冒烟验证（验收 A2~A5）

启动服务：

```powershell
cd d:\Code\PDFClarity_push
python app.py
```

用 PowerShell 验证（另开一个终端）：

```powershell
# A2: 自检状态
Invoke-RestMethod http://127.0.0.1:8000/api/state | ConvertTo-Json
```

预期输出要点：`engine_ok=True`、`gpus` 非空、`gpu_ids` 为选中的编号、`phase="idle"`、`running=False`。

```powershell
# A3: 提交任务(用小 PDF)
$body = @{ pdf = "d:\Code\PDFClarity_push\work\<小样本>.pdf"; scale = 2; target_width = 2160 } | ConvertTo-Json
Invoke-RestMethod -Method Post -Uri http://127.0.0.1:8000/api/jobs -ContentType "application/json" -Body $body | ConvertTo-Json
```

预期：返回 `{"job_id": "...", "status": "started"}`。随后轮询：

```powershell
while ($true) {
  $s = Invoke-RestMethod http://127.0.0.1:8000/api/state
  "{0}  page={1}/{2}  {3}%" -f $s.phase, $s.page, $s.total, $s.percent
  if (-not $s.running) { break }
  Start-Sleep -Milliseconds 800
}
$s | ConvertTo-Json
```

预期：`phase` 依次出现 `extract` → `upscale` → `rebuild` → `done`，且 `output_path` 指向 `output/` 下存在的成品文件。

```powershell
# A4: 防重入(用一个稍大的 PDF,在任务运行中执行)
$body2 = @{ pdf = "d:\Code\PDFClarity_push\work\27张宇基础30讲概率.pdf"; scale = 4; target_width = 4320 } | ConvertTo-Json
try {
  Invoke-RestMethod -Method Post -Uri http://127.0.0.1:8000/api/jobs -ContentType "application/json" -Body $body2
} catch {
  $_.Exception.Response.StatusCode.value__   # 期望 409
}
```

```powershell
# A5: 引擎缺失保护
# 1) 把 bin/realesrgan 临时改名 bin/_realesrgan_backup
# 2) 重启 python app.py
Invoke-RestMethod http://127.0.0.1:8000/api/state | Select engine_ok   # 期望 False
# POST /api/jobs 期望 400
# 3) 验证完恢复目录名
```

> 注意：A5 会触发 `GET /api/state` 返回 `gpus=[]`、`gpu_ids=None`；恢复目录后重启服务即复原。

---

## 8. 风险与注意事项

1. **重构回归风险（S1）**：所有搬移代码必须逐字保留，验收以 §6 的 CLI 对照为准；不要在 M0 顺手改任何流水线行为（含 `print` 文案）；
2. **`HERE` 层级**：`core/paths.py` 的 `dirname(dirname(...))` 是最高频出错点，写完后用一条命令验证：

   ```powershell
   python -c "from core.paths import HERE, ENGINE_EXE, WORK_ROOT, OUT_ROOT; print(HERE); print(WORK_ROOT); print(OUT_ROOT)"
   ```

   期望 `HERE` 打印项目根目录，`WORK_ROOT` 指向 `…\work`；
3. **uvicorn 与 `reload=True`**：M0 用 `uvicorn.run(...)` 时**不要开 `reload`**——任务线程会随 reload 重启被误杀，且 M0 无需热重载；
4. **Windows 多进程提取**：`POST /api/jobs` 由 uvicorn 触发 `scripts/extract_pages.py` 子进程，该子进程内部再开 `ProcessPoolExecutor`（spawn），与 CLI 完全同路径，不受 uvicorn 影响；首次 spawn 较慢属正常（见《CPU 并发加速方案》§四-6）；
5. **任务线程异常兜底**：`_job_thread` 的 `try/except` 保证任何未捕获异常都会落回 `STATE.error`，避免「任务卡死在 running=true」；
6. **daemon 线程与取消**：M0 未实现取消，任务一旦开始只能等其结束；如需中途停止服务，直接 Ctrl+C（daemon 线程随进程退出），可能残留 `_jpg_tmp_*`，重跑时 `process_one` 会清理 src/up，但 `_jpg_tmp_*` 由 rebuild 的临时目录自行清理逻辑处理（rebuild 脚本 exit 时未清理的残留目录，与 CLI 行为一致，M4 统一做残留清理）；
7. **Pydantic 校验**：`scale` 用 `Field(ge=2, le=4)` 只挡「越界值」，不挡「中间值 3」——与 CLI 行为一致（CLI 也不校验 scale=3，引擎会接受），如后续要严格 2/4 校验再加强；
8. **接口返回体积**：`/api/state` 含完整 `metrics` 与 `gpus`，量级极小（KB 级），M0 直接全量返回即可，无需精简字段。

---

## 9. 完成清单（M0 收尾确认）

| 项 | 检查点 | ☐ |
| ---- | ---- | ---- |
| 1 | `core/` 六文件就位，`python -c "from core import process_one"` 通过 | ☐ |
| 2 | `run.py` 薄封装完成，§6 全部回归项通过 | ☐ |
| 3 | `server/` 三文件就位，`python -c "from server.main import app"` 通过 | ☐ |
| 4 | 依赖安装完成（fastapi / uvicorn） | ☐ |
| 5 | A2 自检状态正确 | ☐ |
| 6 | A3 全流水线跑通、`output_path` 成品存在 | ☐ |
| 7 | A4 防重入 409 | ☐ |
| 8 | A5 引擎缺失 400 | ☐ |
| 9 | `git` 提交（如需）：core/ + server/ + app.py + run.py + environment.yml + 本文档 | ☐ |
