# PDFClarity 文件结构说明

> 本文件介绍项目**当前**（清理测试文件与临时文件后）的完整结构，供后续开发 / 排查问题时对照。

---

## 1. 项目概述

把低分辨率 / 在 Skim·预览中显示空白（JPEG2000 + 软蒙版编码）的扫描版 PDF 变清晰：

1. **修复空白**：用 PyMuPDF 重新渲染每页（去掉 JPEG2000），任何阅读器都能打开；
2. **提清晰度**：用 Real-ESRGAN（Vulkan/GPU 加速）对每页做 AI 超分。

实现形态有两种入口，共用同一套核心流水线：

| 入口 | 文件 | 说明 |
|------|------|------|
| CLI | `run.py` | 命令行批量处理，带单行实时状态栏（进度 + GPU/CPU 指标） |
| Web 界面 | `app.py` + `ui/` | FastAPI 后端 + Vue3 前端，浏览器操作，后台线程跑任务 |

---

## 2. 技术栈

- **后端**：Python 3.12+（开发沙箱为 3.14）、FastAPI、Uvicorn、Pydantic
- **前端**：Vue3 + Vite + Pinia + Element Plus（按需引入）
- **核心依赖**：PyMuPDF（渲染/重建）、Pillow、psutil（系统指标）
- **超分引擎**：`bin/realesrgan/realesrgan-ncnn-vulkan`（已内置模型，无需单独安装）
- **通讯模式**：无 WebSocket，前端 0.5~1s 轮询 `/api/state` 与 `/api/logs`（后台线程写全局状态字典，`threading.Lock` 保护）

---

## 3. 目录总览

```
PDFClarity_push/
├── app.py                    # Web 服务入口（启动 FastAPI）
├── run.py                    # CLI 主入口（参数解析 + 批处理编排，Windows/macOS 通用）
├── clear.bat                 # 一键清空 work/ 中间目录（Windows）
├── environment.yml           # conda 环境定义（含 pip 依赖清单）
├── README.md                 # 使用说明
├── 文件结构.md               # 本文档
│
├── __文档__/                 # 各里程碑设计/实现方案文档
│   ├── 图形界面思路.md
│   ├── CPU 并发加速方案.md / .pdf
│   ├── M0具体实现方案.md      # API 骨架
│   ├── M1具体实现方案.md      # 日志轮询 + 静态托管
│   ├── M2具体实现方案.md      # 正式界面一 + 文件上传
│   └── M3具体实现方案.md      # 界面二（日志面板 / 进度条 / 取消）
│
├── core/                     # ★ CLI 与 GUI 共用的核心逻辑
│   ├── __init__.py           # 包入口，统一 re-export 常用符号
│   ├── paths.py              # 全局路径常量（唯一出处，禁止硬编码）
│   ├── gpu.py                # 引擎定位 / Vulkan 设备枚举 / 自动选独显
│   ├── progress.py           # 进度计数与页数探测（纯函数）
│   ├── monitor.py            # SysMonitor：GPU/CPU/内存后台采样
│   ├── statusbar.py          # 终端单行状态栏 + ANSI 配色
│   └── pipeline.py           # 三轮流水线调度 process_one / upscale / run_with_progress
│
├── scripts/                  # 流水线两个子进程脚本
│   ├── extract_pages.py      # [1/3] PDF → 每页 PNG（zoom 2.0，多进程）
│   └── rebuild_pdf.py        # [3/3] 超分后的图 → 干净 PDF
│
├── server/                   # FastAPI 后端
│   ├── __init__.py
│   ├── main.py               # app 定义 + 全部 API 路由 + ui/dist 静态托管
│   ├── jobs.py               # 全局 STATE + 任务线程 + 加权进度 + 取消
│   ├── files.py              # /api/files/inspect、/api/files/upload
│   └── logs.py               # /api/logs 字节偏移增量读取
│
├── ui/                       # Vue3 前端
│   ├── index.html
│   ├── package.json          # 依赖与脚本（dev/build/preview）
│   ├── vite.config.js        # dev 端口 5173，/api 代理到 8000
│   └── src/
│       ├── main.js           # 入口（按需引入 Element Plus）
│       ├── App.vue           # 顶栏（阶段徽标/连接状态）+ 左右布局
│       ├── api/
│       │   ├── client.js     # fetch 封装：超时 + 错误归一化
│       │   └── poller.js     # 轮询器：链式 setTimeout + 指数退避
│       ├── store/
│       │   ├── appState.js   # 状态快照 + 三份日志轮询（offset 游标）
│       │   └── appControl.js # 文件选择 / 参数 / 开始与取消 / 视图切换
│       ├── components/
│       │   ├── Sidebar.vue   # 系统监控（CPU/内存/GPU 峰值）+ 参数区 + 开始按钮
│       │   └── LogPanel.vue  # 日志面板（提取/超分/重建三个 tab，跟随滚动）
│       └── views/
│           ├── SelectPage.vue # 界面一：选文档（拖拽上传 / 粘贴绝对路径）
│           └── WorkPage.vue   # 界面二：工作页（进度条 + 取消 + 更换文档）
│
├── bin/
│   └── realesrgan/           # 超分引擎（随项目分发，勿改动）
│       ├── realesrgan-ncnn-vulkan(.exe)   # 引擎可执行文件
│       ├── vcomp140.dll / vcomp140d.dll   # 运行时库
│       ├── README_windows.md
│       └── models/           # 已内置 6 个模型（x4plus / x4plus-anime / animevideov3 x2~x4）
│
├── vendor/                   # ★ 沙箱环境依赖（fastapi 等，运行必需，勿删）
│   └── fastapi/ ...          # 因沙箱拦截 pip 写系统目录，fastapi 安装在项目内
│
└── vendor_tools/             # ★ PyInstaller 打包工具链（仅打包用）
    └── PyInstaller/ ...
```

> `★` 标记的 `vendor/`、`vendor_tools/` 为第三方依赖/工具，非项目自有代码，但**运行与打包必需**，请不要删除。
> 运行时还会动态生成 `work/`（中间文件）与 `output/`（成品 PDF），均已 git 忽略、不在上树。

---

## 4. 核心流程

### 4.1 三轮流水线（core/pipeline.py 的 `process_one`）

```
输入 PDF
   │
   ▼
[1/3] extract   scripts/extract_pages.py
   │            渲染每页为 PNG（zoom 2.0，去掉 JPEG2000）
   │            输出 work/<书名>/src/page_*.png
   ▼
[2/3] upscale   realesrgan-ncnn-vulkan（GPU）
   │            -i src → -o work/<书名>/up/（x2 或 x4）
   │            进度按输出目录 PNG 数量轮询
   ▼
[3/3] rebuild   scripts/rebuild_pdf.py
   │            超分图 → JPEG(质量90) → 干净 PDF
   │            输出 output/<书名>_clear.pdf
   ▼
成品 PDF
```

关键点：

- 每阶段进度通过轮询**产物文件数量**得到（`core/progress.py`），不解析引擎 stdout；
- 子进程 stdout 写入 `work/<书名>/{extract,upscale,rebuild}.log`，供 Web 端 `/api/logs` 增量读取；
- 取消时 `_kill_tree` 用 `taskkill /T /F` 连子进程树一起终止；
- `rebuild_pdf.py` 顶部 `Image.MAX_IMAGE_PIXELS = 2_000_000_000` 是**红线**，删掉后 4x 大图会触发 PIL DecompressionBomb 崩溃。

### 4.2 Web 端工作方式（轮询模式）

```
浏览器 ── 0.5~1s 轮询 ──▶ GET /api/state   （任务进度 + 系统指标 + 自检结果）
   │                          GET /api/logs?file=extract&offset=N （增量日志）
   │
   └── 用户操作 ──▶ POST /api/jobs            （开始，返回 job_id）
                     POST /api/jobs/{id}/cancel（请求取消，0.4s 内被线程检出）
                     GET /api/files/inspect    （选中文件探测）
                     POST /api/files/upload    （浏览器模式上传到 work/inbox/）
```

- 后台任务线程跑 `process_one`，通过回调把进度写入全局 `STATE`（见 [jobs.py](file:///d:/Code/PDFClarity_push/server/jobs.py)）；
- `jobs.py` 中 `_STAGE_W` 将总进度按 **提取 20% / 超分 70% / 重建 10%** 加权换算；
- 指标线程每 1s 把 `SysMonitor.sample()` 快照写入 `STATE["metrics"]`；
- `server/main.py` 末尾：若存在 `ui/dist`（构建产物）则静态托管，所以生产模式 `npm run build` 后可直接由后端出页面。

---

## 5. 各文件职责详解

### 5.1 根目录

| 文件 | 职责 |
|------|------|
| [app.py](file:///d:/Code/PDFClarity_push/app.py) | Web 服务入口：启动时把 `vendor/` 加入 `sys.path`，然后 `uvicorn.run(app, port=8000)` |
| [run.py](file:///d:/Code/PDFClarity_push/run.py) | CLI 入口：解析参数/环境变量 → GPU 自检与自动选择 → 单文件或批量调用 `process_one`；macOS 下用 `python3 run.py` 调用，启动时自动清除引擎 quarantine 并设置可执行位（原 run.sh 的能力已并入） |
| [clear.bat](file:///d:/Code/PDFClarity_push/clear.bat) | Windows 一键清空 `work/` 中间目录 |
| [environment.yml](file:///d:/Code/PDFClarity_push/environment.yml) | conda 环境定义：python 3.12 + pymupdf/pillow/tqdm/psutil/fastapi/uvicorn |
| [README.md](file:///d:/Code/PDFClarity_push/README.md) | 使用说明（CLI 用法、环境变量、FAQ） |

### 5.2 core/ —— 核心逻辑（CLI 与 GUI 共用）

| 文件 | 职责 |
|------|------|
| [paths.py](file:///d:/Code/PDFClarity_push/core/paths.py) | 项目根、引擎路径、模型目录、`work/`/`output/` 等所有路径常量，其余模块一律从这里导入 |
| [gpu.py](file:///d:/Code/PDFClarity_push/core/gpu.py) | `find_engine`（Windows/macOS 双平台定位引擎）、`probe_gpus`（用小图跑一次引擎枚举 Vulkan 设备）、`pick_gpu`（按名称打分优先独显 NVIDIA） |
| [progress.py](file:///d:/Code/PDFClarity_push/core/progress.py) | `count_png`（统计目录下 PNG 数）、`count_tmp_jpg`（统计 rebuild 临时 jpg 数）、`pdf_page_count`（PyMuPDF 读页数，失败返回 None） |
| [monitor.py](file:///d:/Code/PDFClarity_push/core/monitor.py) | `SysMonitor`：常驻 `nvidia-smi -l 1` 子进程解析 GPU 占用/显存/温度/功耗 + psutil 采样 CPU/内存，后台线程写入带锁缓存 |
| [statusbar.py](file:///d:/Code/PDFClarity_push/core/statusbar.py) | `StatusBar`：TTY 下单行进度条（`\r`+`\033[K` 原地刷新），重定向时退化为每 5% 一行；`enable_ansi` 激活 Windows VT 转义 |
| [pipeline.py](file:///d:/Code/PDFClarity_push/core/pipeline.py) | `process_one`（单个 PDF 全流程，含各阶段回调）、`upscale`、`run_with_progress`（轮询子进程+进度回调+取消检测）、`_kill_tree`（Windows 进程树终止） |
| [__init__.py](file:///d:/Code/PDFClarity_push/core/__init__.py) | 统一 re-export，外部只需 `from core import ...` |

### 5.3 scripts/ —— 流水线子进程脚本

| 文件 | 职责 |
|------|------|
| [extract_pages.py](file:///d:/Code/PDFClarity_push/scripts/extract_pages.py) | `--zoom 2.0` 渲染每页为 PNG（白底 RGB，去掉 JPEG2000/软蒙版）；`--workers` 多进程并行，坏页单块抛错 |
| [rebuild_pdf.py](file:///d:/Code/PDFClarity_push/scripts/rebuild_pdf.py) | 读超分 PNG → 按 `--ref-pdf` 原始页面尺寸重建（处理 /Rotate 旋转）→ JPEG(90) → 输出 PDF；边写页边在 `output/_jpg_tmp_<pid>/` 生成临时 jpg 作为进度依据 |

### 5.4 server/ —— FastAPI 后端

| 文件 | 职责 |
|------|------|
| [main.py](file:///d:/Code/PDFClarity_push/server/main.py) | FastAPI app（version 0.4.0）；lifespan 里 `jobs.init()`/`shutdown()`；路由：`/api/state`、`/api/jobs`（POST，409 防重入/400 引擎缺失）、`/api/jobs/{id}/cancel`、`/api/logs`、`/api/files/inspect`、`/api/files/upload`；末尾静态托管 `ui/dist` |
| [jobs.py](file:///d:/Code/PDFClarity_push/server/jobs.py) | 全局 `STATE` 字典 + `snapshot()`（深拷贝）；`start_job` 持锁置 running 后 spawn 任务线程；加权进度 `_weighted_percent`（20/70/10）；`request_cancel` 置位由任务线程 0.4s 内检出；`init()` 设子进程 UTF-8 编码并做自检（引擎/GPU/nvidia-smi） |
| [files.py](file:///d:/Code/PDFClarity_push/server/files.py) | `inspect(path)`：存在性/页数/成品路径推导；`upload`：500MB 上限、流式写盘到 `work/inbox/`、超限删除半成品并返回 413 |
| [logs.py](file:///d:/Code/PDFClarity_push/server/logs.py) | `read(pdf, file, offset)`：白名单映射文件名防路径穿越，按字节偏移增量读（单次最多 256KB），offset 超文件大小自动归零 |

### 5.5 ui/ —— Vue3 前端

| 文件 | 职责 |
|------|------|
| [main.js](file:///d:/Code/PDFClarity_push/ui/src/main.js) | 入口：createApp + Pinia，Element Plus 按需引入（不再全量 use） |
| [App.vue](file:///d:/Code/PDFClarity_push/ui/src/App.vue) | 顶栏（品牌 + 阶段徽标 + 连接状态）+ Sidebar + 主区视图切换（select/work） |
| [api/client.js](file:///d:/Code/PDFClarity_push/ui/src/api/client.js) | 统一 fetch 封装：10s 超时（AbortController）、错误归一化（超时/断连/HTTP detail 解析）；上传走 60s 超时 |
| [api/poller.js](file:///d:/Code/PDFClarity_push/ui/src/api/poller.js) | 轮询器：链式 `setTimeout` 不重叠，失败指数退避（0.8s→10s），`setOnError` 注入错误回调 |
| [store/appState.js](file:///d:/Code/PDFClarity_push/ui/src/store/appState.js) | 持有 `/api/state` 快照 + 三份日志数组与字节 offset 游标；轮询 `/api/state` 与三个 `/api/logs`；job_id 变化时重置日志 |
| [store/appControl.js](file:///d:/Code/PDFClarity_push/ui/src/store/appControl.js) | 选中文件（`selectByPath` 带 busy 防连点）、参数表（scale/target_width/model/gpu_ids/extract_workers）、`start()`（G1 成品已存在确认框）、`cancelJob()`、视图切换 |
| [components/Sidebar.vue](file:///d:/Code/PDFClarity_push/ui/src/components/Sidebar.vue) | 系统监控卡（CPU/内存/GPU 峰值）+ 参数区（放大倍数/目标宽度/模型/GPU/提取进程数）+ 开始按钮（状态机：idle→ready→running，done/error 可重跑） |
| [components/LogPanel.vue](file:///d:/Code/PDFClarity_push/ui/src/components/LogPanel.vue) | 日志面板：提取/超分/重建三个 tab，当前阶段圆点标识、超限截断提示、复制全部、跟随滚动/回到底部、错误行高亮 |
| [views/SelectPage.vue](file:///d:/Code/PDFClarity_push/ui/src/views/SelectPage.vue) | 界面一：双通道选文件——拖拽/点击上传（浏览器模式）与粘贴绝对路径探测（桌面壳模式，同一 `selectByPath` 链路） |
| [views/WorkPage.vue](file:///d:/Code/PDFClarity_push/ui/src/views/WorkPage.vue) | 界面二：LogPanel + 底部进度条（`el-progress` 中间文字显示 `[类型] 正在xx第 x/y 页`）+ 用时/预计剩余 + 取消/更换文档按钮 |
| [vite.config.js](file:///d:/Code/PDFClarity_push/ui/vite.config.js) | dev 端口 5173，`/api` 代理到 `http://127.0.0.1:8000`；Element Plus 按需解析插件 |

---

## 6. 运行方式

```powershell
# CLI（在项目根）
python run.py <输入.pdf 或 文件夹> [放大倍数] [目标宽度]

# Web 后端（需 fastapi/uvicorn；若用沙箱 vendor，先设 PYTHONPATH 指向 vendor）
python app.py                # → http://127.0.0.1:8000

# Web 前端开发态（另开终端）
cd ui
npm install                  # 首次
npm run dev                  # → http://127.0.0.1:5173（/api 已代理到 8000）
```

---

## 7. 运行时目录（自动生成，不入库）

| 目录 | 内容 | 清理方式 |
|------|------|----------|
| `work/` | 每本书的 `src/`（原始页 PNG）、`up/`（超分页 PNG）、`*.log`；`inbox/`（上传的 PDF） | `clear.bat` 或直接删除 |
| `output/` | 成品 `<书名>_clear.pdf`，以及 rebuild 过程中的 `_jpg_tmp_<pid>/` 临时目录（结束自动清） | 保留成品，临时目录可删 |

---

## 8. 已清理内容（本次整理）

为保持仓库整洁，已删除以下**测试/临时文件**（均为明确标注"验证后删除"或运行时产物，不影响任何功能）：

- `_m3_progress_check.py` —— C1/C2 加权进度与消息格式断言脚本
- `_stage_deps.py` —— C11 PyInstaller 依赖暂存脚本
- `dist/PDFClarity_smoke.exe` —— 冒烟测试打包产物
- `work/` 全部内容 —— 运行中间文件（含输入 PDF，处理完可重新拷入）
- 所有 `__pycache__/` —— Python 字节码缓存（运行时自动重建）
